# UriPack: urimessage

Thin Markpact — capabilities from [`urimessage/urimessage/manifest.yaml`](urimessage/urimessage/manifest.yaml).
Handlers: existing ``python://`` refs (code in `urimessage/`, not duplicated here).

Generated by `urisys/scripts/generate_pack_markpacts.py`.

```yaml markpact:pack
apiVersion: urisys.io/v1
kind: UriPack
metadata:
  id: urimessage
  version: '1'
  language: python
description: Simple alert/message dispatch mock for lab and node.
schemes:
- message
capabilities:
- id: message-alert-send
  uri: message://local/alert/command/send
  kind: command
  operation: message.alert.send
  handler: python://urimessage.handlers:alert_send
  side_effects: true
  approval: required
policy:
  default: deny_mutations_without_approval
runtime:
  default_environment: real
  supports:
  - mock
  - local
  - docker
  expose:
  - pack
  - service
  - flow
  - interface
  - adapter
```

```yaml markpact:run
scheme: message
default: flow
modes:
- pack
- service
- flow
- interface
- adapter
service:
  port_hint: 8789
  path: /uri/call
flow:
  ids:
  - message-smoke
uses: []
adapter:
  call: POST /uri/call
  events: GET /events
```

```yaml markpact:tests
tests:
- id: message.alert.send_command
  uri: message://local/alert/command/send
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: message.alert.send
  payload: {}
```

```yaml markpact:flow id=message-smoke
flow:
  id: message-smoke
  description: Smoke message:// routes (generated from manifest).
defaults:
  approved: true
  dry_run: true
do:
- message://local/alert/command/send: {}
```

```markdown markpact:docs
## Unpack → `.markpact/`

```bash
cd urimessage
export TELLMESH_ROOT=/home/tom/github/tellmesh
PACK=markpacts/urimessage.markpact.md
urisys markpact run "$PACK" --as flow --approve --dry-run
urisys markpact run "$PACK" --as pack
urisys markpact run "$PACK" --as service --port 8789
urisys markpact run "$PACK" --as interface
urisys markpact run "$PACK" --as adapter
```

- Source manifest: `urimessage/urimessage/manifest.yaml`
- Requires: `pip install -e urimessage` or `TELLMESH_ROOT` for python:// handlers
```
